// ============================================================
// ShopFlow — Privacy Policy Page Logic
// ============================================================

document.addEventListener('DOMContentLoaded', () => {
  renderHeader();
  renderFooter();
});
